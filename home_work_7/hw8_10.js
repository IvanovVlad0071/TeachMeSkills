const users = ['Danya', , 'Vika', 'Grisha', 'Fedya', 'Nikita']
for (let i = 0; i < users.length; i++) {
    if (users[2] != null) {
        elem3 = users[2]
    }
    else
        elem3 = "eee"
    if
        (users[1] != null) {
        elem2 = users[1]
    }
    else
        elem2 = "bbb"
}
console.log(elem2);
console.log(elem3);